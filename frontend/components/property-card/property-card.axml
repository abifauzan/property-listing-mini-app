<view class="property-card flex flex-col overflow-hidden" onTap="onTapCard">
  <view class="property-card__image-container">
    <image
      class="property-card__image"
      src="{{imageUrl}}"
      mode="aspectFill"
      lazy-load="{{true}}"
      onError="onImageError"
    />
  </view>
  <view class="property-card__content flex flex-col flex-1">
    <text class="property-card__title">{{property.title}}</text>
    <text class="property-card__price">{{property.price}}</text>
  </view>
</view>
